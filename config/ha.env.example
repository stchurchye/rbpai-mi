# 复制为 ha.env 并填写（勿提交 Git）
# HA 个人资料 → 安全 → 长期访问令牌

HA_URL=http://192.168.1.100:8123
HA_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.REPLACE_ME

# 可选：仅拉取 entity_id 含以下关键词的传感器（逗号分隔，留空=全部温湿度源实体）
HA_ENTITY_FILTER=xiaomi,miot,miaomiaoce

# 可选：手机推送服务（开发者工具 → 服务 → notify）
HA_NOTIFY_SERVICE=notify.notify
